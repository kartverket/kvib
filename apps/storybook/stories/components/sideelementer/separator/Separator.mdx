import { Box, Code, Text } from "@kvib/react";
import { Canvas, Controls, Meta, Story } from "@storybook/blocks";
import { DocsAnatomy, DocsContainer, DocsHeading, DocsStory, Feedback } from "../../../templates";
import * as SeparatorStories from "./Separator.stories";

<Meta of={SeparatorStories} />

<DocsContainer>

# Separator

Separator brukes til å visuelt separere innhold i en liste eller gruppe.

```jsx
import { Divider } from "@kvib/react";
```

<DocsStory
  title="Separator-varianter"
  description={
    <Text>
      Bruk <code>variant</code> prop for å endre <code>Divider</code> varianten.
    </Text>
  }
  story={<Canvas of={SeparatorStories.DividerVariants} sourceState="hidden" className="fullwidth" />}
/>

<DocsStory
  title="Farger og bredde"
  description={
    <Text>
      Farger og bredde på <code>Divider</code> kan bli manipulert gjennom <code>borderColor</code>og{" "}
      <code>borderWidth</code> prop.
    </Text>
  }
  story={<Canvas of={SeparatorStories.DividerColorsWidths} sourceState="hidden" className="fullwidth" />}
/>

<DocsStory
  title="Orientation"
  description={
    <Text>
      Bruk <code>orientation</code>prop for å sette vertikal <code>Divider</code>.
    </Text>
  }
  story={<Canvas of={SeparatorStories.DividerVertical} sourceState="hidden" className="fullwidth" />}
/>

<DocsStory
  title="Innhold i Separator"
  description={
    <Text>
      Eksempel på innhold i <code>Divider</code> ved bruk av <code>Box</code>, <code>Divider</code>,{" "}
      <code>AbsoluteCenter</code> og <code>Text</code> komponenter.
    </Text>
  }
  story={<Canvas of={SeparatorStories.DividerContent} sourceState="hidden" className="fullwidth" />}
/>

<DocsStory
  isVertical
  title="Props"
  description={""}
  story={
    <>
      <Canvas of={SeparatorStories.Preview} className="fullwidth" />
      <Controls of={SeparatorStories.Preview} />
    </>
  }
/>

</DocsContainer>
