import { Text } from "@kvib/react/src";
import { Canvas, Controls, Meta, Story } from "@storybook/blocks";
import { DocsAnatomy, DocsContainer, DocsHeading, DocsStory, Feedback } from "../../../templates";
import * as CircularProgressStories from "./CircularProgress.stories";

<Meta of={CircularProgressStories} />

<DocsContainer>

# Circular Progress

Circular Progress brukes til å indikere framgang i en prosess.

```jsx
import { CircularProgress, CircularProgressLabel } from "@kvib/react";
```

<Feedback component="CircularProgess" />

<DocsStory
  title="Størrelser"
  description={
    <Text>
      Bruk <code>size</code> prop for å endre størrelse på komponenten.
    </Text>
  }
  story={<Canvas of={CircularProgressStories.CircularProgressSizes} sourceState="hidden" />}
/>

<DocsStory
  title="Tykkelse"
  description={
    <Text>
      Bruk <code>thickness</code> prop for å endre tykkelse på komponenten.
    </Text>
  }
  story={<Canvas of={CircularProgressStories.CircularProgressThickness} sourceState="hidden" />}
/>

<DocsStory
  title="Farger"
  description={
    <Text>
      Bruk <code>color</code> prop for å endre farge på komponenten.
    </Text>
  }
  story={<Canvas of={CircularProgressStories.CircularProgressColors} sourceState="hidden" />}
/>

<DocsStory
  title="Label"
  description={
    <Text>
      Bruk <code>CircularProgressLabel</code> for å sette label på komponenten.
    </Text>
  }
  story={<Canvas of={CircularProgressStories.CircularProgressLabel} sourceState="hidden" />}
/>

<DocsStory
  title="Ubestemt Progress"
  description={
    <Text>
      Bruk <code>isIndeterminate</code> prop for å sette ubestemt progress på komponenten.
    </Text>
  }
  story={<Canvas of={CircularProgressStories.CircularProgressIndeterminate} sourceState="hidden" />}
/>

<DocsHeading light>Props</DocsHeading>
<Canvas of={CircularProgressStories.Preview} />
<Controls of={CircularProgressStories.Preview} />

</DocsContainer>
