import { Alert, AlertDescription, AlertIcon, AlertTitle } from "@kvib/react/src";
import { Canvas, Controls, Meta } from "@storybook/blocks";
import * as ScaleFadeStories from "./ScaleFade.stories";

<Meta of={ScaleFadeStories} />

# Fade

  <Alert>OBS! Animasjonen kan være hakkete i Storybook </Alert>

## Ta i bruk

```jsx
import { ScaleFade } from "@kvib/react";
```

## Props

  <Alert status="info">
    <AlertIcon />
    <AlertTitle>OBS!</AlertTitle>
    <AlertDescription>Trykk på ```in``` prop for å se animasjonen</AlertDescription>
  </Alert>

<Canvas of={ScaleFadeStories.Preview} />
<Controls of={ScaleFadeStories.Preview} />
