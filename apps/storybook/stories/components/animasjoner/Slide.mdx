import { Alert, AlertDescription, AlertIcon, AlertTitle, KvibProvider } from "@kvib/react/src";
import { Canvas, Controls, Meta } from "@storybook/blocks";
import * as SlideStories from "./Slide.stories";

<Meta of={SlideStories} />

# Slide

Denne animasjonen sklir elementer inn fra valgt retning.

## Ta i bruk

```jsx
import { Slide } from "@kvib/react";
```

## Props

<KvibProvider>
  <Alert status="info">
    <AlertIcon />
    <AlertTitle>OBS!</AlertTitle>
    <AlertDescription>Trykk på ```in``` prop for å se animasjonen</AlertDescription>
  </Alert>
</KvibProvider>

<Canvas of={SlideStories.Preview} />
<Controls of={SlideStories.Preview} />
