import { Alert, AlertDescription, AlertIcon, AlertTitle, KvibProvider } from "@kvib/react/src";
import { Canvas, Controls, Meta } from "@storybook/blocks";
import * as FadeStories from "./Fade.stories";

<Meta of={FadeStories} />

# Fade

Animasjonen gjør elementer gradvis siktig/gjennomsiktig

## Ta i bruk

```jsx
import { Fade } from "@kvib/react";
```

## Props

<KvibProvider>
  <Alert status="info">
    <AlertIcon />
    <AlertTitle>OBS!</AlertTitle>
    <AlertDescription>Trykk på ```in``` prop for å se animasjonen</AlertDescription>
  </Alert>
</KvibProvider>

<Canvas of={FadeStories.Preview} />
<Controls of={FadeStories.Preview} />
