import { Canvas, Controls, Meta, Story } from "@storybook/blocks";
import * as ShowStories from "./Show.stories";

<Meta of={ShowStories} />

# Show

Show er en wrapperkomponent som render eller ikke render child-elementer hvis en gitt betingelse er sann.

```jsx

## Ta i bruk

```jsx
import { Show } from "@kvib/react";
```

## Props

<Canvas of={ShowStories.Preview} />
<Controls of={ShowStories.Preview} />
