import { Alert, AlertIcon, Box, Code, Text } from "@kvib/react/src";
import { Canvas, Controls, Meta } from "@storybook/blocks";
import { DocsAnatomy, DocsContainer, DocsHeading, DocsStory, Feedback } from "../../../templates";
import * as FormControlStories from "./Form-control.stories";

<Meta of={FormControlStories} />

<DocsContainer>

# Form Control

Form Control gir kontekst som «isInvalid», «isDisabled» og «isRequired» til skjema elementer

## Ta i bruk

```jsx
import { FormControl, FormLabel, FormErrorMessage, FormHelperText } from "@kvib/react";
```

<Feedback component="FormControl" />

<DocsStory
  title="FormControl"
  description={
    <Text>
      Form Control gir kontekst som <Code>isInvalid</Code>, <Code>isDisabled</Code> og <Code>isRequired</Code> til
      skjema elementer
    </Text>
  }
  story={<Canvas of={FormControlStories.Preview} sourceState={"hidden"} />}
/>
<DocsStory
  title="FormControl States"
  description={""}
  story={<Canvas of={FormControlStories.State} sourceState={"hidden"} />}
/>

<DocsStory
  title="FormControl Invalid State"
  description={
    <Text>
      <Code>FormErrorMessage</Code> vil kun vises når <Code>isInvalid</Code> i <Code>FormControl</Code> er
      <Code>true</Code>
    </Text>
  }
  story={<Canvas of={FormControlStories.Error} sourceState={"hidden"} />}
/>

<DocsHeading light>Props</DocsHeading>
<Canvas of={FormControlStories.Preview} />
<Controls of={FormControlStories.Preview} />

</DocsContainer>
