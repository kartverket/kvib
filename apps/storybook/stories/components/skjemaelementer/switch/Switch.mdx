import { Text } from "@kvib/react/src";
import { Canvas, Controls, Meta } from "@storybook/blocks";
import { DocsContainer, DocsHeading, DocsStory, Feedback } from "../../../templates";
import * as SwitchStories from "./Switch.stories";

<Meta of={SwitchStories} />

<DocsContainer>

# Switch

Switch brukes som et alternativ for checkbox.

## Ta i bruk

```jsx
import { Switch } from "@kvib/react";
```

<Feedback component={"Switch"} />

<DocsStory
  title={"Bruk"}
  description={
    <Text>
      <code>Switch</code> må alltid følge med et <code>label</code>, og følge den samme tastaturarbeidsflyten som en{" "}
      <code>checkbox</code>.
    </Text>
  }
  story={<Canvas of={SwitchStories.Preview} sourceState="hidden" />}
/>
<DocsStory
  title={"Størrelser"}
  description={
    <Text>
      <code>Switch</code> har 3 størrelser, bruk <code>size</code> prop for å endre størrelsen på komponenten.
    </Text>
  }
  story={<Canvas of={SwitchStories.SwitchSizes} sourceState="hidden" />}
/>
<DocsStory
  title={"Farger"}
  description={
    <Text>
      Bakgrunnsfarge kan endres ved å bruke <code>colorScheme</code> prop.
    </Text>
  }
  story={<Canvas of={SwitchStories.SwitchColors} sourceState="hidden" />}
/>
<DocsStory
  title={"States"}
  description={
    <Text>
      <code>Switch</code> tilstand som <code>isDisabled</code> har en innvirkning på brukervennligheten og på stilen av{" "}
      <code>Switch</code>, bortsett fra <code>isInvalid</code> og <code>isRequired</code> props
    </Text>
  }
  isVertical
  story={<Canvas of={SwitchStories.SwitchStates} sourceState={"hidden"} />}
/>

<DocsHeading light>Props</DocsHeading>
<Canvas of={SwitchStories.Preview} />
<Controls of={SwitchStories.Preview} />

</DocsContainer>
