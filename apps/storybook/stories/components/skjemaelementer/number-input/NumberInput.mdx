import { Box, Code, Stack, Text } from "@kvib/react";
import { Canvas, Controls, Meta } from "@storybook/blocks";
import { DocsAnatomy, DocsContainer, DocsHeading, DocsStory, Feedback } from "../../../templates";
import * as NumberInputStories from "./NumberInput.stories";

<Meta of={NumberInputStories} />

<DocsContainer>

# NumberInput

NumberInput ligner på Input-komponenten, men den har kontroller for å øke eller redusere numeriske verdier.

```jsx
import {
  NumberInput,
  NumberInputField,
  NumberInputStepper,
  NumberIncrementStepper,
  NumberDecrementStepper,
} from "@kvib/react";
```

**Egnet til:**

- Svar
- Tall med eller uten interaktive knapper

**Uegnet til:**

- Søk (bruk heller `Search` eller `SearchAsync`)
- Interaktiv datepicker (bruk heller `DatePicker`)


<DocsHeading light>Props</DocsHeading>
<Canvas of={NumberInputStories.Preview} className="sb-unstyled" />
<Controls of={NumberInputStories.Preview} />

</DocsContainer>
