import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as CollapseStories from "./Collapse.stories";
import { KvibProvider, Alert, AlertIcon, AlertTitle, AlertDescription } from "@kvib/react";

<Meta of={CollapseStories} />

# Collapse

Animasjonen virker som en type accordion/utvidbart panel. startingHeight kan bli brukt for en type "vis mer" effekt.

## Ta i bruk

```jsx
import { Collapse } from "@kvib/react";
```

## Props

<KvibProvider>
  <Alert status="info">
    <AlertIcon />
    <AlertTitle>OBS!</AlertTitle>
    <AlertDescription>Trykk på ```in``` prop for å se animasjonen</AlertDescription>
  </Alert>
</KvibProvider>

<Canvas of={CollapseStories.Collapse} />
<Controls of={CollapseStories.Collapse} />
