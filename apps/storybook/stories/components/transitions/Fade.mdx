import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as FadeStories from "./Fade.stories";
import { KvibProvider, Alert, AlertIcon, AlertTitle, AlertDescription } from "@kvib/react/src";

<Meta of={FadeStories} />

# Fade

Animasjonen gjør elementer gradvis siktig/gjennomsiktig

## Ta i bruk

```jsx
import { Fade } from "@kvib/react";
```

## Props

<KvibProvider>
  <Alert status="info">
    <AlertIcon />
    <AlertTitle>OBS!</AlertTitle>
    <AlertDescription>Trykk på ```in``` prop for å se animasjonen</AlertDescription>
  </Alert>
</KvibProvider>

<Canvas of={FadeStories.Fade} />
<Controls of={FadeStories.Fade} />
