import { Title, Canvas, Meta, Story, Controls } from "@storybook/blocks";
import * as TextStories from "./Text.stories";

<Meta of={TextStories} />

# Text

<Canvas of={TextStories.Text} />

## Ta i bruk

```jsx
import { Text } from "@kvib/react";
```

### Viktig! `fontSize` og `textStyle` må ikke brukes samtidig! fontSize-verdien vil overskrive fontstørrelsen i textStylen.

## Props

<Controls of={TextStories.Text} />
