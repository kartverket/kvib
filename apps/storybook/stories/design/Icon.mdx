import { Meta, Canvas, Controls } from "@storybook/addon-docs";
import * as IconStories from "./Icon.stories";

<Meta of={IconStories} />

# Icon

Vi benytter ikoner som et virkemiddel for hjelpe brukere med å forstå handling eller kontekst. Ikoner kan være med på å
skille og fremheve innhold.
Det er også et fint navigasjonsverkøy som kan vise varsling av feil eller fortelle brukeren om de forlater nettsiden.
[Her er det noen guidelines for bruk av ikoner](https://www.intechnic.com/blog/icon-usability-best-ux-tips-and-design-guidelines/)

Kartverkets designsystem tilbyr et ikonelement som viser og lar deg style ikoner fra Google sin Material Symbols Rounded.
Gå til [Material symbols rounded sine sider](https://fonts.google.com/icons?icon.set=Material+Symbols&icon.style=Rounded)
for å få en oversikt over ikoner du kan benytte.

## Ta i bruk

```jsx
import { Icon } from "@kvib/react";
```

## Props

<Canvas of={IconStories.defaultIcon} />
<Controls of={IconStories.defaultIcon} />

# Eksempel for fill og farger

Man kan enten bruke vanlige css argumenter (fargenavn, hex-koder, RGBA osv.), men det er også mulig å hente farger fra Designsystemet på denne måten:

```jsx
import { Icon, colors } from "@kvib/react";

<Icon icon="home" color={colors.green[400]} />;
```

OBS! Enkelte ikoner har ikke en "filled" variant.

<Canvas of={IconStories.colorIcon} />

# Eksempel for størrelser

<Canvas of={IconStories.sizeIcon} />

# Eksempel for weight prop

<Canvas of={IconStories.weightIcon} />

# Eksempel for grade prop

<Canvas of={IconStories.gradeIcon} />
