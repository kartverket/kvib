import { Alert, AlertDescription, AlertIcon, Box } from "@kvib/react/src";
import { Meta } from "@storybook/addon-docs";
import { Canvas, Source } from "@storybook/blocks";
import { DocsContainer, DocsHeading, DocsStory, Feedback } from "../templates";
import { TokenExample } from "./TokenExample";
import { TokenStrings } from "./srcTokenStrings";

<Meta title="Hooks/useToken"/>

# useToken

**useToken**

```jsx
import { useToken } from "@kvib/react";
```

<Feedback component="useToken"/>

<DocsContainer>

<DocsHeading light>Return value</DocsHeading>
**useToken** henter det som er i temaet på de gitte stiene (paths).

<DocsHeading light>Bruk</DocsHeading>

    <TokenExample/>
    <Source code={TokenStrings} dark/>
</DocsContainer>

<Alert status="info">
    <AlertIcon/>
    <Box>
        <AlertDescription>
            Merk at du også kan bruke **sx**-egenskapen for å skrive stiler fritt ved å bruke temaets tokens
        </AlertDescription>
    </Box>
</Alert>

