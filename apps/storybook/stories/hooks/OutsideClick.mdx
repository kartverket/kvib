import { Meta } from "@storybook/addon-docs";
import {DocsHeading, DocsStory, DocsContainer, Feedback} from "../templates";
import {Canvas, Source} from "@storybook/blocks";
import { Alert, AlertIcon, Box, AlertDescription } from "@kvib/react"
import {OutsideClickExample} from "./OutsideClickExample";
import {OutsideClickStrings} from "./srcOutsideClickStrings";



<Meta title="Hooks/useOutsideClick" />

# useOutsideClick

**useOutsideClick** er en tilpasset hook som håndterer klikkhendelser utenfor et bestemt DOM-element, som for eksempel en **div**. En håndterer kalles når en klikk- eller trykkehendelse skjer utenfor det refererte elementet.

<Alert status="warning">
    <AlertIcon />
    <Box>
        <AlertDescription>
            Denne hooken er kompatibel med mus- og trykkehendelser.
        </AlertDescription>
    </Box>
</Alert>

```jsx
import { useOutsideClick } from "@kvib/react";
```

<Feedback component="useOutsideClick"/>

<DocsContainer>
   # Bruk
    <Canvas>
        <OutsideClickExample/>
    </Canvas>
    <Source code={OutsideClickStrings} dark/>
</DocsContainer>


