import { defineRecipe } from "@chakra-ui/react";

export const inputGroupTheme = defineRecipe({
  className: "kvib-input-group",
  base: {
    paddingInlineStart: "0",
    paddingInlineEnd: "0",
  },
});
